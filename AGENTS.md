# AGENTS 규칙

## 언어

* 대화는 기본적으로 한국어로 한다.
* 코드 작성은 기본적으로 영어로 한다. 단, 주석은 한국어로 한다.
* 고유 명사는 원문 그대로 사용한다.

## 문자

* ASCII 를 벗어난 특수문자는 가능한 사용하지 않는다. ASCII 안에서 표현 가능하면 ASCII 를 최우선으로 사용한다.

## 모듈

* 모든 스니펫은 루트 `go.mod` 단일 모듈을 공유한다. `go test ./...` 한 번으로 전부 도는 단순함이 목적이다.
* 무거운 의존성(예: k8s client-go)이 들어와도 모듈을 쪼개지 않는다. 쪼갤 필요가 생기면 그때 별도로 논의한다.
